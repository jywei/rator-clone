<% if @member.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@member.errors.count, "error") %> prohibited this member from being saved:</h2>

  <ul>
  <% @member.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
</div>
<%end%>
  <div class="col-md-8">
    <div class="form-group">
      <%= m.label :name, "姓名:"%>
      <%= m.text_field :name, :class => "form-control"%>
    </div>
    <div class="form-group">
    <%= m.label :office, "職位:"%>
    <%= m.text_field :office, :class => "form-control" %>
    </div>
    <div class="form-group">
      <%= m.label :about, "關於我about:"%>
      <%= m.text_area :about, :class => "form-control", :rows=>"8"%>
    </div>

    <div class="form-group">
    <%= m.submit "送出", :class => "btn btn-success"%>
    </div>
  </div>


  <div class="col-md-4 padding_r">
    <div class="image">
      <div class="field">
          <%= m.label :photo, "photo 130x130" %><br>
          <%= m.file_field :photo%>
          <%= m.label :photo_alt, "photo_alt" %><br>
          <%= m.text_field :photo_alt%>
      </div>
    </div>
    <br>
    <%if @member.photo.present?%>
      <%=link_to image_tag(@member.photo.url(:thumb)), @member.photo.url, :target=>"_blank"%>
      <%= @member.photo_alt%>
    <%end%>
  </div>

