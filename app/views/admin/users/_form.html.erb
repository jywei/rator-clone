<% if @user.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

  <ul>
  <% @user.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
</div>
<%end%>

    <div class="form-group">
      <%= u.label :name, "name:"%>
      <%= u.text_field :name, :class => "form-control"%>
    </div>

    <div class="form-group">
      <%= u.label :nickname, "nickname:"%>
      <%= u.text_field :nickname, :class => "form-control"%>
    </div>

    <div class="form-group">
      <%= u.label :sex, "sex:"%>
      <%= u.select :sex, user_sexs_options , { :include_blank => t(:select_prompt) }, :class => "form-control" %>
    </div>

    <div class="form-group">
      <%= u.label :email, "email:"%>
      <%= u.email_field :email, :class => "form-control"%>
    </div>

    <div class="form-group">
      <%= u.label :phone, "phone:"%>
      <%= u.phone_field :phone, :class => "form-control"%>
    </div>

    <div class="form-group">
      <%= u.label :address, "address:"%>
      <%= u.text_field :address, :class => "form-control"%>
    </div>

    <div class="form-group">
      <%= u.label :birthday, "birthday:"%>
      <%= u.date_select :birthday, start_year: 1900, end_year: 2020, include_blank: true%>
    </div>

    <div class="form-group">
    <%= u.submit "送出", :class => "btn btn-success"%>
    </div>